{
  "name": "quant-desk",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Quant Desk — grinder + judge + a propose-only chat. Demo-only research bench for the Trading Journal app. No execution code; the model only ever proposes JSON.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "import": "node scripts/import_gma.js",
    "probe": "node scripts/probe-model.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "better-sqlite3": "^11.10.0",
    "express": "^4.22.2",
    "js-yaml": "^4.3.2",
    "zod": "^4.5.4"
  }
}
