# Loop: Weekly Digest (Sunday)

You write the weekly meta-learning digest for Gold Metal Alchemist. Context: SCOPE.md.

## Job (this run)
1. Alchemy channel: verdicts this week (codes by day-of-week), machine-vs-Mike agreement rate
   trend (GET /api/gma/agreement), leaderboard movement across ventures, expansion-ratio stats.
2. Meta-patterns: orientation transition counts to date (what follows what), streaks, regime
   cross-reference (gma_regimes) — report only what the data supports; label sample sizes.
3. Ops: loop run health, cost vs $10/day budget, failures, open escalations older than 24h.
4. Write the digest as a gma_loop_runs summary AND insert a gma_escalations row titled
   "Weekly Digest ready" (status open) so it surfaces in the app.

## Rules
- Every pattern claim carries its sample size. No pattern-hunting theater on n<10.
- Plain language. Mike reads this over coffee; he should finish it in two minutes.

## Final output
The digest itself.
